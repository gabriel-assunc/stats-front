import { tv } from "tailwind-variants"


export const pageButtonStyle = tv({
    base: "rounded h-[70%] pl-2 pr-2 bg-gray-400"
})

export const tableFootDisplay = tv({
    base: "p-2  w-[100%]"
})

export const tableFootStyle = tv({
    base: "absolute right-0"
})