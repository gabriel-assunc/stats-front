import { tv } from "tailwind-variants"

export const popupDisplay = tv({
    base: 'w-full h-[160%] static p-2 bg-slate-50 text-black rounded z-10 hover:cursor-default'
})

export const chartDisplay = tv({
    base: 'border-solid border-2 border-slate-200 w-full h-full'
})