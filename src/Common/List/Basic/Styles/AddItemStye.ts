import { tv } from "tailwind-variants";
export const addDisplay = tv({
    base: 'border-double border-4 border-red-500 w-[4%] h-[7%] '
})

export const addStyle = tv({
    base: 'w-full h-full relative hover:cursor-pointer'
})