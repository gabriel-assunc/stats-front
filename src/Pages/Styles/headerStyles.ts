import { tv } from "tailwind-variants"

export const headerStyle = tv({
    base: 'w-[5%] h-[8%] z-10 flex justify-center p-2'
})

export const iconStyle = tv({
    base: 'h-full w-full p-1 items-center rounded-full bg-[#0D1B2A] hover:bg-[#1B263B] text-white hover:cursor-pointer'
})