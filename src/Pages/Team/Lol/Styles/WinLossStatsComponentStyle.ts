import { tv } from "tailwind-variants"

export const winLossStatsDisplay = tv({
    base: 'h-[27%] flex flex-col'
})
export const winLossStatsContainer = tv({
    base: 'h-[90%] relative bg-[#415A77] flex flex-col space-y-2 pt-2 pb-2 pl-1 pr-1 w-full rounded'
})

export const labelDisplay = tv({
    base: 'relative left-2'
})