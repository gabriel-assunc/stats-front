import { tv } from "tailwind-variants"

export const geralStyleDisplay = tv({
    base: 'h-[18%] relative rounded flex flex-col'
})

export const geralStatContainer = tv({
    base: 'h-full w-full rounded bg-[#415A77] pt-2 pb-2 pl-1 pr-1'
})

export const labelDisplay = tv({
    base: 'z-10 flex flex-row items-center space-x-2 relative left-2'
})